# Campaign 2026-08-25 judge summary

Verdict: **pass**. Mean 4.45, min 3, no hallucinated configs, `llm_calls=0` on all 18 repos. Every reported percentage is an exact rounding of n/35 (39 criteria minus 4 `requiresLLM` skips), and all four goldens' remediation lists reproduce the deterministic sort in `recommend()` exactly.

Original-4 ranking: `ranking_ok_internal` true, `ranking_ok_expected` **false**. chat-example 9% < express 34% = fastapi 34% < act 63%. fastapi ties express instead of landing MID-HIGH, so the harness rule that fastapi may not fall to or below express is violated. The cause is catalog resolution, not scoring: fastapi configures ruff and mypy in `pyproject.toml` (which the catalog reads for `formatter` but not for `linter` or `type-checker`), ships `uv.lock` (not in the lock-file list), and keeps its contributing guide at `docs/en/docs/contributing.md`. All three reproduce against synthetic fixtures. Level sanity is docked to 3 for fastapi only.

Language coverage: 8 languages across 18 repos — JavaScript (5), Python (3), Go (3), TypeScript (2), Java (2), Rust, C, Haskell. Only zod and act reach Level 2.

Gate mechanics and the L1 cap: 39 criteria minus the 4 LLM-only rows leaves 35 counted checks across five sequential levels holding 4, 10, 12, 8, and 1 rows. A level clears at 80% of its own counted rows, skipped rows leave the denominator instead of counting as fails, and the walk stops at the first level that does not clear — so the reported level is the last consecutive level cleared, floored at 1 (Foundational, Guided, Structured, Optimized, Autonomous). Level 1 holds four rows (`editorconfig`, a README of 500+ characters, `lock-file`, `license`), and 80% of 4 rounds up to all four, so one low-signal miss stops the walk at the first step. 15 of 18 repos have at least one L1 fail, concentrated in `editorconfig` (10) and `lock-file` (9) while `license` (2) and `readme` (1) are rare. A fixture missing only `.editorconfig`, with every L2 row satisfied, scored 49% and stayed Level 1 — cli/cli and prettier at 63% are the same shape. Among repos with no L1 fail, the L2 gate is decided by the dev-environment rows (`env-documentation`, `setup-script`, `version-pinned`).

Top 3 optimize items:

1. Broaden `lock-file` (uv.lock, pdm.lock, cabal.project.freeze, stack.yaml.lock, gradle verification-metadata, mix.lock) and skip it for languages with no lock convention. Catalog list edit plus the existing skipped state; clears 9 of 18 L1 failures.
2. Make the L1 gate survivable: add a fifth L1 criterion so 4 of 5 clears, or expose a `capReason` in `maturity_level` so the canvas explains a Level 1 cap instead of implying broad immaturity.
3. Teach `linter` and `type-checker` to read `pyproject.toml` and `setup.cfg` (`[tool.ruff.lint]`, `[tool.mypy]`, `[tool.pyright]`). Fixes the fastapi band miss directly. Full scores: `eval/campaign-2026-08-25-judge.json`.
