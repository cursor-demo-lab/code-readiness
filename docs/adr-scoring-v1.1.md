# ADR: scoring contract v2

Supersedes the v1.1 identity band (L1 75% of readme / license / lock-file).

## Decision

Align **level placement of existing check ids** with Factory Agent Readiness. Unlock remains 80% of counted rows at the current level. Skipped rows leave the denominator. The walk is still sequential; minimum level is 1. No new criterion ids. No new pillars. No LLM scoring. No L4/L5 retune except ids that left those levels.

- **`thresholdForLevel` is 0.80 for every level, including L1.** The 0.75 L1 special case is gone. `catalog.level1Threshold` is 0.8.
- **Labels (engine-owned):** 1 Functional, 2 Documented, 3 Standardized, 4 Optimized, 5 Autonomous. Canvas may print these strings from report JSON; it does not recompute the gate.
- **L1 Functional:** `readme`, `linter`, `test-files-exist`, `type-checker`. 80% of 4 requires 4/4. When `type-checker` skips, 80% of 3 requires 3/3. License and lock-file are not an L1 identity band.
- **`type-checker` skip.** Keep `languagesPass` for Go / Rust / Java / Kotlin / C# / Swift when a **manifest** is present (`go.mod`, `Cargo.toml`, `pom.xml`/`build.gradle`, `build.gradle.kts`, `*.csproj`/`global.json`, `Package.swift`). Pass on mypy / pyright files, `setup.cfg` `[mypy]`, pyproject `[tool.mypy]` / `[tool.pyright]`, or a root `tsconfig.json` (existence; `"strict": true` is an extra hit, not a gate). If none of those hit, skip (JavaScript without tsconfig, Python without mypy/pyright, stray `*.go` without `go.mod`, and similar). File/config hits run before `languagesPass`. Skip drops from the L1 denominator.
- **L2 Documented:** `license` and `lock-file` move here (lock-file keeps the JS/TS/Python/Java/C/Haskell skip-when-absent). `ai-context` and `pre-commit-hooks` move here from L3. Also: `editorconfig`, `formatter`, `test-framework`, `test-script`, `contributing`, `env-documentation`, `setup-script`, `version-pinned`, `ci-config`.
- **L3 Standardized:** `api-docs`, `codeowners`, `issue-templates`, `containerization`, `ci-runs-tests`, `ci-runs-linters`, `build-automated`, `no-outdated-deps`, `security-policy`, `dep-update-automation`.
- **Python-native L2 detectors (unchanged from this branch).** `test-script` still accepts `scripts.test` / Makefile `test`, plus `scripts/test` / `scripts/test.sh` / `scripts/test-*`, tox/nox/pytest.ini, or pyproject `[tool.pytest` / `[tool.tox` / `[tool.hatch.envs`. `setup-script` still accepts package.json `scripts.dev|test|lint|build`, Makefile `setup|install`, a root Makefile, plus `scripts/install*`, `setup.py` / `setup.cfg`, or pyproject `[build-system]`.

## Level map (existing ids only)

| Level | Label | Counted ids |
| --- | --- | --- |
| 1 | Functional | readme, linter, test-files-exist, type-checker |
| 2 | Documented | license, lock-file, editorconfig, formatter, test-framework, test-script, contributing, env-documentation, setup-script, version-pinned, ci-config, ai-context, pre-commit-hooks |
| 3 | Standardized | api-docs, codeowners, issue-templates, containerization, ci-runs-tests, ci-runs-linters, build-automated, no-outdated-deps, security-policy, dep-update-automation |
| 4 | Optimized | coverage-config, e2e-tests, architecture-docs, deploy-pipeline, branch-protection, dead-code-detection, security-scanning, secrets-detection |
| 5 | Autonomous | bundle-analysis (plus skipped LLM rows) |

Non-AI counts: L1=4 need 4 (3/3 when type-checker skips), L2=13 need 11, L3=10 need 8, L4=8 need 7, L5=1 need 1.

## Why

Factory L1 Functional examples are README, linter, type checker, and unit tests. Factory L2 Documented examples include AGENTS.md and pre-commit. Our previous L1 75% identity band (readme / license / lock) was the lie: a repo could look Functional and still sit at band 1 for a missing lockfile, or look unfinished and clear L1 on license+lock alone.

Factory L3 examples include FastAPI. The earlier Python-native `test-script` / `setup-script` work stays: pytest/tox/hatch and `scripts/test*` / `[build-system]` are real signals, not JS/Makefile-only shapes.

Hill-climb after the v2 27-OSS eval: Factory L1 Functional treats type checker as "a checker exists," not TypeScript `strict: true`. Scoring v2 failed nestjs/nest and colinhacks/zod for a present non-`strict` `tsconfig.json`, and a JS/TS compiler stub passed as Go from `tools/*.go` because `languagesPass` ran first and `detectLanguages` treated `*.go` as enough. Existence of root `tsconfig.json` now passes; `languagesPass` requires a manifest; file/config hits precede `languagesPass`. No new ids, no threshold change, no new pillars.

Hill-climb after PR #12: a JS/TS compiler stub dropped L2→L1 because Go formatter no longer false-passed from stray `tools/*.go`. The remaining formatter fail is a detector hole — that stub has a root `.dprint.jsonc` and still failed `formatter` with "No formatter configuration found." Catalog `anyFiles` listed Prettier / rustfmt / rubocop; `fileContains` listed biome / black / ruff / package.json `"prettier"`. dprint is a real formatter that repo uses, and `.prettierrc.*` does not match `.dprint.jsonc`. Root `.dprint.json` / `.dprint.jsonc` / `dprint.json` now pass `formatter`. No new ids, no dummy `.editorconfig`, no L1/L2 threshold change (stays 80%; 80% of 12 needs 10; that stub was 9/12 and dprint makes that 10/12 Documented honestly), no new pillars.

Hill-climb after PR #13: tj/commander.js is 9/12 L2 (need 10 for 80%) and fails `pre-commit-hooks`, `test-framework`, and `ai-context`. The test-framework fail is a detector hole — the stub has `scripts.test = "node --test && npm run check:type:ts"` and `tests/*.test.js`, but catalog `fileContains` for package.json included the token `"node --test"` WITH wrapping quotes, so `evalFileContains` looks for quote-node-space-dash-dash-test-quote. After `--test` comes space then `&&`, not a closing quote; `node:test` is also absent. Unquoted `node --test` now matches. Do not treat `**/*.test.js` as test-framework (that is `test-files-exist`, L1). Do not dummy AGENTS.md or pre-commit. `packageJsonPath` `scripts.test` stays on `test-script`, a different criterion. No new ids, no L1/L2 threshold change, no new pillars.

Hill-climb after PR #14: nestjs/nest is L1 Functional 69% at L2 10/13 (need 11). Remaining L2 fails: editorconfig, ai-context, env-documentation. env-documentation FAILs `No .env.example or similar found` because compose files exist. The nest stub has `integration/docker-compose.yml` plus `sample/**/docker-compose.yml` and `sample/**/*.env`. There is no root `.env`, `.env.example`, or root compose. Nested sample/integration compose was counting as "this library has env to document." `hasEnvSignals` now counts only root-level `.env` / `.env.*` / `.envrc` / `docker-compose.y*ml` / `compose.y*ml`. Nested `sample/**` / `examples/**` / `integration/**` compose or env do not trigger the fail path. A repo with root `.env` or root compose and no `.env.example` still FAILs. A repo with no env/compose/direnv at root still SKIPS. Do not dummy `.editorconfig`. nest 10/13 becomes 10/12 Documented honestly. No new ids, no L1/L2 threshold change, no new pillars.

Hill-climb after PR #16: huggingface/transformers and huggingface/diffusers fail `version-pinned` (`No runtime version pin found.`) at L2 8/11 (need 9). Their stub `pyproject.toml` files are byte-identical to GitHub and have no `requires-python` (not truncated). Their stub `setup.py` files are 0 bytes vs GitHub 12998 / 11262. Upstream `setup.py` contains `python_requires` (`>=3.10.0`). Catalog `version-pinned` `fileContains` only looked at pyproject `requires-python` (plus `.python-version` / engines / `go.mod`); it never read `setup.py`. `setup.py` `python_requires` now passes. Do not treat ruff `target-version` as a runtime pin. Do not dummy `.editorconfig`. Eval stubs of transformers/diffusers `setup.py` are truncated and must be refreshed for the promotion to show on the 27-OSS campaign; stubs live outside this repo. No new ids, no L1/L2 threshold change, no new pillars.

Hill-climb catalog coverage: broaden existing detectors only so each Factory pillar recognizes common language-native files (`anyFiles` / `fileContains` / `anyGlobs` / `languagesPass`, plus `CI_GLOBS` / `TEST_FILE_GLOBS`). Eval-grounded extras from the n=27 campaign: Error Prone and Cargo `[workspace.lints` / `[lints.rust`; Spotless / php-cs-fixer / yapf as formatters only; rust skip-when-no-`Cargo.lock`; `**/tests/**/*.rs` (nested crate tests); mocha `test/*.js`; `Cargo.toml` / `pom.xml` as setup-script; Maven/Gradle compiler pins. No new ids, no new pillars, no L4/L5 retune, no LLM rows, no dummy `.editorconfig`, no canvas restyle. Formatters are not linters. ESLint and Biome stay equal-rank JS/TS linters; golangci-lint configs include toml/json. Env-documentation skip stays root-only. `.cursor/environment.json` stays with PR #19. Do not rescore the 27-OSS campaign in this repo.

Hill-climb after PR #17: `editorconfig` is an L2 Documented fail that caps OSS libraries (nest, gin, httpx, transformers, fastapi, …) even when they already have a real linter (eslint, biome, ruff, oxlint, golangci-lint, …). A linter is a machine-runnable style contract. `.editorconfig` is a weaker indent/charset hint. Dummy `.editorconfig` is parked. When `linter` PASSES and `.editorconfig` is missing, `editorconfig` SKIPS (drop from the L2 denominator, third state, not a pass). When `linter` fails or is skipped, `editorconfig` still FAILs if missing and PASSES if `.editorconfig` exists. Do not skip `editorconfig` merely because a formatter exists. `linter` is language-native, not ESLint-shaped: add `.flake8` / `.pylintrc` / `pylintrc`, `clippy.toml` / `.clippy.toml`, `.clang-tidy`, `.hlint.yaml` / `hlint.yaml`, `.credo.exs`, `.tflint.hcl`, `.shellcheckrc`, `.luacheckrc`, `.jshintrc`; pyproject `[tool.pylint` / `[tool.flake8`; setup.cfg `flake8` / `pylint`; Cargo.toml `[lints.clippy` / `[workspace.lints` / `[lints.rust`; package.json `"oxlint"` / `"standard"` / `"xo"`. Do not treat formatters as linters (no Prettier, Black, rustfmt, gofmt, clang-format). No new ids, no dummy `.editorconfig`, no L1/L2 threshold change, no new pillars. Do not rescore the 27-OSS campaign in this repo; gin/httpx may promote L2 if remaining fails after this skip still hit 80%; nest stays L2 with `editorconfig` no longer in `l2_fails`.

Hill-climb: `containerization` (L3 Developer Environment) also passes on `.cursor/environment.json`. That path is Cursor-specific — Cloud Agents resolve the repo file first, then personal/team saved environments — not a new criterion id. Existence is enough, same as Dockerfile; a root `environment.json` does not count. Nest still fails this unless the stub actually has the file (it does not). No new ids, no dummy `.editorconfig`, no L4/L5 retune, no new pillars.

Hill-climb after PR #56: nest and phoenix PASS `containerization` on `integration/docker-compose.yml` / `integration_test/docker-compose.yml`. That is a test harness, not a product boot environment. Product boot is `.devcontainer` / `.devcontainer/devcontainer.json` / `.cursor/environment.json` / a root `Dockerfile` / `docker-compose.yml` / `compose.yml`. First-hit now prefers those files and defers `tests/**` / `test/**` / `integration/**` / `integration_test/**` the way formatter defers `tests/format`. An integration-only tree still PASSES and names that file. Message-only when a product boot also exists. Do not fail nest/phoenix for integration-only compose. No new ids, no dummy `.editorconfig`, no L4/L5 retune, `issue-templates` stays L3 (10 counted).

Hill-climb after PR #59: fmtlib/fmt PASSes `setup-script` on `support/build.gradle` (Android NDK sidecar) even though root `CMakeLists.txt` exists. Catalog `anyFiles` lists `build.gradle` before `CMakeLists.txt`, so first-hit named the NDK library. First-hit now prefers the shallowest product-tree setup file and defers `support/**` / `android/**` / `examples/**` the way containerization defers `integration/**`. A support-only tree still PASSES and names that file. Message-only. Do not fail fmt `setup-script`. `test-script` still FAILs `support/build.gradle` (not a runner); do not put `build.gradle` back on `test-script`. Stay L1 because `test-script` FAILs. No new ids, no dummy `.editorconfig`, no L4/L5 retune, `issue-templates` stays L3, `codeowners` stays Security.

Hill-climb after PR #60: a C# tree PASSes `test-script` on `Src/Lib.FuzzTests/Lib.FuzzTests.csproj` because walk order and same-depth lex sort hit FuzzTests before `Src/Lib.Tests/Lib.Tests.csproj`. Both match `**/*Tests.csproj`. First-hit among those `*Tests.csproj` / `*Test.csproj` / `*Tests.sln` hits now defers a basename containing `Fuzz` / `fuzz` / `Benchmark` / `bench` when another Tests project exists (`Lib.Tests.csproj` over `Lib.FuzzTests.csproj`). A Fuzz-only tree still PASSES. Message-only. Do not restore product `*.csproj` on `test-script`. Stay L1 because `linter` FAILs. No new ids, no dummy `.editorconfig`, no L4/L5 retune, `issue-templates` stays L3, `codeowners` stays Security.

Hill-climb after PR #61: prettier/prettier PASSes `issue-templates` on `.github/ISSUE_TEMPLATE/config.yml` even though `formatting.md` / `integration.md` exist. `config.yml` is the GitHub issue chooser sidecar, not a template an agent would fill. First-hit now prefers a form (`bug_report.md` / `Bug_report.yml` / `formatting.md`) over `config.yml` / `config.yaml` when both exist; the shallowest form wins. A config.yml-only tree still PASSES and names that file. Message-only. Empty `.github/ISSUE_TEMPLATE/` is still not a hit (`skipDirectoryHits`). Do not fail `docs/ISSUE_TEMPLATE.md` or root `ISSUE_TEMPLATE.md`. Stay L3. No new ids, no dummy `.editorconfig`, no L4/L5 retune, `codeowners` stays Security, `test-script` still defers FuzzTests, `setup-script` still prefers product-tree first-hit.

Hill-climb after PR #63: nestjs/nest PASSes `test-framework` on `vitest.config.coverage.mts` even though `vitest.config.mts` exists. Both match `vitest.config.*`; same-depth lex sort hits `coverage` first. First-hit among those `vitest.config.*` / `jest.config.*` hits now defers a basename containing `coverage` / `coverage.` / `integration` when another product runner exists (`vitest.config.mts` over `vitest.config.coverage.mts` / `vitest.config.integration.mts`). A coverage-only or integration-only tree still PASSES. Sample-only trees still pass via product-tree first-hit. Message-only. Do not dummy `.editorconfig`. Stay L3. No new ids, no L4/L5 retune, `issue-templates` stays L3, `codeowners` stays Security, `setup-script` product first-hit and FuzzTests defer stay. `[tool.ty]` / `[tool.basedpyright]` still pass `type-checker`.

Hill-climb after PR #64: a JS/TS tree PASSes `type-checker` on `packages/foo/test/tsconfig.json` even though `packages/foo/tsconfig.json` exists. Walk order / test-path hit the suite config first. First-hit among `tsconfig.json` / `jsconfig.json` hits now defers a path with a whole segment named `test` / `tests` / `spec` / `__tests__` when another tsconfig exists outside those segments (`packages/foo/tsconfig.json` over `packages/foo/test/tsconfig.json`). Nested product tsconfigs still count. A test-only tree still PASSES and names that file. Message-only. Do not add `**/tsconfig.json` to anyFiles. Do not dummy `.editorconfig`. Stay L2. No new ids, no L4/L5 retune, `issue-templates` stays form-over-config, FuzzTests defer, setup-script product first-hit, `[tool.ty]` / `[tool.basedpyright]`, and vitest coverage defer stay.

Hill-climb after PR #65: prettier/prettier PASSes `issue-templates` on `.github/PULL_REQUEST_TEMPLATE.md` even though `formatting.md` exists. After dropping `config.yml`, `shallowestHit` prefers the 2-segment PR template over the 3-segment issue form. First-hit now prefers an issue form (`.github/ISSUE_TEMPLATE/**` except `config.yml` / `config.yaml`, plus `.github/ISSUE_TEMPLATE.md`) over a PR template when both exist; the shallowest form wins (`Bug_report.yml` / `formatting.md` / `bug_report.md`). A PR-template-only tree still PASSES and names that file. A config.yml-only tree still PASSES. Empty `.github/ISSUE_TEMPLATE/` still FAILs (`skipDirectoryHits`). Message-only. Stay L3. Do not dummy `.editorconfig`. No new ids, no L4/L5 retune. Style & Validation, `[tool.ty]` / `[tool.basedpyright]`, FuzzTests defer, setup-script product first-hit, vitest coverage defer, and type-checker product tsconfig stay.

Hill-climb after PR #66: nestjs/nest PASSes `containerization` on `sample/05-sql-typeorm/docker-compose.yml` (TypeORM example MySQL sidecar) after #58 deferred `integration/`. `sample/` is in the style-config defer list, not in container first-hit defer. First-hit now defers `sample` / `samples` / `example` / `examples` the way formatter / test-framework do. Product boot (`.devcontainer` / `.cursor/environment.json` / root Dockerfile/compose) still wins. After dropping integration AND sample, leftover hits still PASS (sample-only or integration-only). When only deferred hits remain, first-hit is the shallowest leftover (`integration/docker-compose.yml` over a deeper sample compose). Message-only. Do not fail nest for sample-only/integration-only. Do not dummy `.editorconfig`. Stay L3. No new ids, no L4/L5 retune. `isIssueForm` / `productIssueTemplateHits` stay.

Hill-climb after PR #68: eslint still names a test-path tsconfig and react first-hit is a fixture tsconfig. PR #65 defers whole `test` / `tests` / `spec` / `__tests__` segments, but not `fixtures` / `testdata` already used by style-config first-hit. First-hit among `tsconfig.json` / `jsconfig.json` now also defers those style-config segments when another product tsconfig exists (`packages/foo/tsconfig.json` over `fixtures/tsconfig.json`; root `tsconfig.json` over `testdata/tsconfig.json`). A fixtures-only or testdata-only tree still PASSES. Product `packages/foo/tsconfig.json` still wins over `packages/foo/test/tsconfig.json`. `[tool.ty]` still PASSES. Message-only. Do not add `**/tsconfig.json` to anyFiles. Do not dummy `.editorconfig`. Stay L2. No new ids, no L4/L5 retune. `issue-templates` stays form-over-PR-template. Container first-hit still spreads `STYLE_FIRST_HIT_SAMPLE_SEGMENTS`.

Hill-climb after PR #67: a C# tree PASSes `setup-script` on `Src/Lib.FuzzTests/Lib.FuzzTests.csproj` even though `Src/Lib/Lib.csproj` exists. Same-depth lex: `Src/Lib.` < `Src/Lib/`. This REGRESSED vs v35 (product `Lib.csproj`) after `test-script` started naming `Tests.csproj`. First-hit among `*.csproj` / `*.sln` hits now defers a basename containing `Fuzz` / `fuzz` / `Benchmark` / `bench` when another csproj exists, and prefers the product project (`Lib.csproj`) over `*.Tests.csproj` / `*.FuzzTests.csproj` when both exist (Tests projects are runners, already on `test-script`). A Fuzz-only or Tests-only tree still PASSES. Message-only. Do not restore product `*.csproj` onto `test-script`. Keep #60 product-tree defer (`support` / `android` / `examples`), #61 `test-script` Fuzz defer, #66 `isIssueForm`, #68 containerization sample defer, and #67 type-checker fixtures/testdata defer. Stay L1 because `linter` FAILs. No new ids, no dummy `.editorconfig`, no L4/L5 retune, `issue-templates` stays L3, `codeowners` stays Security.

Hill-climb after PR #69: a C# tree FAILs `test-framework` even though `Src/Lib.Tests/Lib.Tests.csproj` exists (xUnit/NUnit test project). Catalog had no C# hit: no `*Tests.csproj`, no `fileContains` xunit/nunit on `*.csproj`. `test-script` already PASSes on `*Tests.csproj`. `test-framework` now passes on `**/*Tests.csproj` / `**/*Test.csproj` (same runners as `test-script`) or a `*.csproj` that contains `xunit` / `nunit` / `MSTest`. First-hit among those Tests projects reuses the `test-script` Fuzz/Benchmark defer (`Lib.Tests.csproj` over `Lib.FuzzTests.csproj`). A Fuzz-only tree still PASSES. Product `Foo.csproj` is not a framework (still `setup-script` only). Do not dummy `.editorconfig`. Stay L1 because `linter` FAILs. No new ids, no L4/L5 retune. Keep #66 issue form over PR template, #67 type-checker fixtures/testdata, #68 containerization sample defer, and #69 setup-script product csproj first-hit. `issue-templates` stays L3. `codeowners` stays Security.

Hill-climb after PR #70: a JS/TS-primary tree PASSes `test-files-exist` and `test-framework` on `tools/customlint/plugin_test.go` even though thousands of `*.test.ts` files exist. `**/*_test.go` is a language-native glob so it ranked above the `**/*.test.*` catch-all; walk order named the sidecar Go test. First-hit now prefers hits matching the detected product language over a sidecar language. When `detectLanguages` includes typescript/javascript, prefer `*.ts` / `*.tsx` / `*.js` / `*.mjs` tests over `*_test.go`. Count still includes Go tests. A Go-only test tree still PASSES. `test-framework` first-hit that names a test file follows the same preference. Message-only. Do not dummy `.editorconfig`. Stay L2. No new ids, no L4/L5 retune. Keep #66 issue form, #67 type-checker fixtures/testdata, #68 containerization sample defer, #69 product csproj over FuzzTests, and #70 C# `*Tests.csproj` / xunit / nunit / MSTest.

Hill-climb after PR #71: phoenixframework/phoenix PASSes `test-framework` on `jest.config.js` (JS assets sidecar) even though Mix `*_test.exs` / `test/test_helper.exs` exist. Same class of lie as a JS/TS-primary tree naming `plugin_test.go`. When the tree is Mix/Elixir-primary (`mix.exs` present and `**/*_test.exs` / `**/*_spec.exs` / `test/test_helper.exs` exist), first-hit prefers those ExUnit files over `jest.config.*` / `vitest.config.*`. A JS-only jest tree still PASSES. A Mix tree with only jest (no exs tests) still PASSES on jest. Message-only. Do not dummy `.editorconfig`. Stay L2. No new ids, no L4/L5 retune. Keep #66 issue form, #67 type-checker fixtures/testdata, #68 containerization sample defer, #69 product csproj over FuzzTests, #70 C# `*Tests.csproj` / xunit / nunit / MSTest, and #71 JS/TS-over-Go TFE rank.

Hill-climb after PR #72: a JS/TS-primary tree PASSes `linter` on root `.golangci.yml` with no ESLint. That file is real (Go sidecar tools), but first-hit is a Go linter in a JS/TS-primary compiler because `.golangci.yml` sorts before `eslint.config.js`. When both a JS/TS linter (`eslint.config.*` / `biome.json` / `.oxlintrc.json`) and golangci exist, first-hit prefers the JS/TS linter in a JS/TS-primary tree (`detectLanguages` has typescript/javascript) and prefers golangci in a Go-primary tree (`go.mod` without `package.json` / `tsconfig.json`). A golangci-only tree still PASSES and names that file. Do not fail a JS/TS-primary tree for lacking eslint. Message-only. Stay L2. No dummy `.editorconfig`. No new ids, no L4/L5 retune. Keep #66–#72. `productTestFrameworkHits` Mix defer (`withoutJsSidecar` / `deferJsFrameworkSidecarHits`) stays.

Hill-climb after PR #73: phoenixframework/phoenix PASSes `test-files-exist` on `assets/*.test.js` even though Mix `*_test.exs` exist. Same class of lie as #71 (JS/TS over sidecar Go) and #72 (ExUnit over jest config): Mix/Elixir-primary trees may name a JS assets test as first-hit. When the tree is Mix/Elixir-primary (`mix.exs` present and `**/*_test.exs` / `**/*_spec.exs` / `test/test_helper.exs` exist), first-hit prefers those ExUnit files over `*.test.js` / `*.spec.ts` / `*.test.ts`. Count still includes JS tests. A Mix tree with only JS tests still PASSES and names the JS file. A JS-only tree is unchanged. JS/TS-primary still prefers `*.ts` over `*_test.go`. Message-only. Do not dummy `.editorconfig`. Stay L2. No new ids, no L4/L5 retune. Keep #66–#73 including Mix test-framework ExUnit over jest and `productLinterHits` / `isJsTsLinterFile` / `isGolangciFile` / `treeIsGoPrimary`.

Hill-climb after PR #74: Rails/RSpec-primary trees may name a JS assets test as `test-files-exist` first-hit even when `**/*_spec.rb` / `**/*_test.rb` exist. Same class of lie as #74 Mix ExUnit over JS tests. When the tree is Rails/RSpec-primary (`Gemfile` present and `**/*_spec.rb` / `**/*_test.rb` / `test/test_helper.rb` exist), first-hit prefers those Ruby files over `*.test.js` / `*.spec.ts` / `*.test.ts`. Count still includes JS tests. A Rails tree with only JS tests still PASSES and names the JS file. A JS-only tree is unchanged. Mix/Elixir-primary still prefers `*_test.exs` over JS. JS/TS-primary still prefers `*.ts` over `*_test.go`. Message-only. Do not dummy `.editorconfig`. Stay L2. No new ids, no L4/L5 retune. Keep #66–#74 including Mix ExUnit TFE rank, Mix test-framework ExUnit over jest, and `productLinterHits` / `isJsTsLinterFile` / `isGolangciFile` / `treeIsGoPrimary`.

Hill-climb after PR #76: Mix/Elixir-primary trees may name a JS assets prettier/biome config as `formatter` first-hit even when root `.formatter.exs` exists (same class as #72 jest vs ExUnit). When `mix.exs` and `.formatter.exs` exist, first-hit prefers `.formatter.exs` over `prettier.config.*` / `.prettierrc*` / `biome.json`. A Mix tree with only prettier still PASSES on prettier. A JS-only prettier tree is unchanged. Message-only. Stay L2. No dummy `.editorconfig`. No new ids, no L4/L5 retune. Keep #66–#76. `deferJsTestSidecarHits` / `deferJsTestSidecarForRuby` / `isRubyTestFile`, Mix TFE ExUnit rank, and `productLinterHits` stay.

Hill-climb after PR #75: Rails/RSpec-primary trees may name `jest.config.js` / `vitest.config.*` as `test-framework` first-hit even when `*_spec.rb` / `*_test.rb` / `spec/spec_helper.rb` exist. Same class of lie as #72 Mix ExUnit over jest. When the tree is Rails/RSpec-primary (`Gemfile` present and `**/*_spec.rb` / `**/*_test.rb` / `spec/spec_helper.rb` / `test/test_helper.rb` exist), first-hit prefers those Ruby files over `jest.config.*` / `vitest.config.*`. A Rails tree with only jest still PASSES on jest. A JS-only jest tree is unchanged. Mix/Elixir-primary still prefers ExUnit over jest. Rails TFE still prefers `*_spec.rb` over `*.test.js`. Message-only. Do not dummy `.editorconfig`. Stay L2. No new ids, no L4/L5 retune. Keep #66–#76 and #75 `productFormatterHits` / `deferJsFormatterSidecarHits` / `isMixFormatterFile` / `isJsFormatterSidecar`. Keep #76 `deferJsTestSidecarHits` / `deferJsTestSidecarForRuby` / `isRubyTestFile`.

Hill-climb after PR #77: Python-primary trees may name a JS assets test as `test-files-exist` first-hit even when pytest/unittest `test_*.py` / `*_test.py` exist. Same class of lie as #74 Mix ExUnit and #76 Rails RSpec. When the tree is Python-primary (`pyproject.toml` or `setup.py` / `setup.cfg` present and `**/test_*.py` / `**/*_test.py` / `tests/conftest.py` exist), first-hit prefers those Python files over `*.test.js` / `*.spec.ts` / `*.test.ts`. Count still includes JS tests. A Python tree with only JS tests still PASSES and names the JS file. A JS-only tree is unchanged. Mix/Elixir-primary still prefers `*_test.exs` over JS. Rails/RSpec-primary still prefers `*_spec.rb` over JS. Rails test-framework still prefers RSpec over jest. JS/TS-primary still prefers `*.ts` over `*_test.go`. Message-only. Do not dummy `.editorconfig`. Stay L2. No new ids, no L4/L5 retune. Keep #66–#77 and #75 including Mix ExUnit TFE rank, Rails TFE rank, Rails test-framework over jest, Mix formatter over prettier, and `productLinterHits` / `isJsTsLinterFile` / `isGolangciFile` / `treeIsGoPrimary`.

Hill-climb after PR #78: Python-primary trees may name `jest.config.js` / `vitest.config.*` as `test-framework` first-hit even when `pytest.ini` / `conftest.py` / `test_*.py` exist. Same class of lie as #72 Mix ExUnit over jest and #77 Rails RSpec over jest. When the tree is Python-primary (`pyproject.toml` or `pytest.ini` present and `**/test_*.py` / `**/*_test.py` / `**/conftest.py` exist), first-hit prefers those Python files over `jest.config.*` / `vitest.config.*`. A Python tree with only jest still PASSES on jest. A JS-only jest tree is unchanged. Mix/Elixir-primary still prefers ExUnit over jest. Rails/RSpec-primary still prefers `*_spec.rb` over jest. Mix formatter stays. Python TFE helpers stay (`deferJsTestSidecarHits` / `deferJsTestSidecarForPython` / `isPythonTestFile` / `treeHasPythonManifest`). Message-only. Do not dummy `.editorconfig`. Stay L2. No new ids, no L4/L5 retune. Keep #66–#78 and #75 `productFormatterHits` / `deferJsFormatterSidecarHits`. Keep #77 Rails test-framework RSpec defer.

Hill-climb after PR #79: Java-primary trees may name a JS assets test as `test-files-exist` first-hit even when `*Test.java` / `*Tests.java` exist. Same class of lie as #74 Mix ExUnit, #76 Rails RSpec, and #78 Python pytest. When the tree is Java-primary (`pom.xml` or `build.gradle` / `build.gradle.kts` present and `**/*Test.java` / `**/*Tests.java` exist), first-hit prefers those Java files over `*.test.js` / `*.spec.ts` / `*.test.ts`. Count still includes JS tests. A Java tree with only JS tests still PASSES and names the JS file. A JS-only tree is unchanged. Mix/Elixir-primary still prefers `*_test.exs` over JS. Rails/RSpec-primary still prefers `*_spec.rb` over JS. Python-primary still prefers `test_*.py` over JS. Python test-framework still prefers pytest over jest (`deferJsFrameworkSidecarHits` includes `deferJsTestSidecarForPython` and `pytest.ini`). JS/TS-primary still prefers `*.ts` over `*_test.go`. Message-only. Do not dummy `.editorconfig`. Stay L2. No new ids, no L4/L5 retune. Keep #66–#79 and #75 including Mix ExUnit TFE rank, Rails TFE rank, Python TFE rank, Python test-framework over jest, Rails test-framework over jest, Mix formatter over prettier, and `productLinterHits` / `isJsTsLinterFile` / `isGolangciFile` / `treeIsGoPrimary`.

Hill-climb after PR #80: Java-primary trees may name `jest.config.js` / `vitest.config.*` as `test-framework` first-hit even when `*Test.java` / `*Tests.java` / `src/test` exist. Same class of lie as #72 Mix ExUnit over jest, #77 Rails RSpec over jest, and #79 Python pytest over jest. When the tree is Java-primary (`pom.xml` or `build.gradle` present and `**/*Test.java` / `**/*Tests.java` exist), first-hit prefers those Java files over `jest.config.*` / `vitest.config.*`. A Java tree with only jest still PASSES on jest. A JS-only jest tree is unchanged. Mix/Elixir-primary still prefers ExUnit over jest. Rails/RSpec-primary still prefers `*_spec.rb` over jest. Python-primary still prefers `test_*.py` over jest. Mix formatter stays. Java TFE helpers stay (`deferJsTestSidecarHits` / `deferJsTestSidecarForJava` / `isJavaTestFile` / `treeHasJavaManifest`). Message-only. Do not dummy `.editorconfig`. Stay L2. No new ids, no L4/L5 retune. Keep #66–#80 and #75 `productFormatterHits` / `deferJsFormatterSidecarHits`. Keep #79 Python and #77 Rails test-framework language ranks.

Hill-climb after PR #81: C#-primary trees may name a JS assets test as `test-files-exist` first-hit even when `*Tests.cs` / `*Test.cs` exist. Same class of lie as #74 Mix ExUnit, #76 Rails RSpec, #78 Python pytest, and #80 Java TFE. When the tree is C#-primary (`*.csproj` or `*.sln` present and `**/*Tests.cs` / `**/*Test.cs` exist), first-hit prefers those C# files over `*.test.js` / `*.spec.ts` / `*.test.ts`. Count still includes JS tests. A C# tree with only JS tests still PASSES and names the JS file. A JS-only tree is unchanged. First-hit among those C# tests reuses the `test-script` Fuzz/Benchmark basename defer (`FooTests.cs` over `FooFuzzTests.cs`). A Fuzz-only C# tree still PASSES. Mix/Elixir-primary still prefers `*_test.exs` over JS. Rails/RSpec-primary still prefers `*_spec.rb` over JS. Python-primary still prefers `test_*.py` over JS. Java-primary still prefers `*Test.java` over JS. Java test-framework still prefers `*Test.java` over jest (`deferJsFrameworkSidecarHits` includes `deferJsTestSidecarForJava`; catalog `**/*Test.java` / `**/*Tests.java`). Mix formatter stays. Message-only. Do not dummy `.editorconfig`. Stay L2. No new ids, no L4/L5 retune. Keep #66–#81 and #75 including Mix ExUnit TFE rank, Rails TFE rank, Python TFE rank, Java TFE rank, Java test-framework over jest, Python test-framework over jest, Rails test-framework over jest, Mix formatter over prettier, and `productLinterHits` / `isJsTsLinterFile` / `isGolangciFile` / `treeIsGoPrimary`.

Hill-climb after PR #82: C#-primary trees may name `jest.config.js` / `vitest.config.*` as `test-framework` first-hit even when `*Tests.cs` / `*Test.cs` exist. Same class of lie as #72 Mix ExUnit over jest, #77 Rails RSpec over jest, #79 Python pytest over jest, and #81 Java over jest. When the tree is C#-primary (`*.csproj` / `*.sln` present and `**/*Tests.cs` / `**/*Test.cs` exist, excluding Fuzz/Benchmark), first-hit prefers those C# files over `jest.config.*` / `vitest.config.*`. A C# tree with only jest still PASSES on jest. A JS-only jest tree is unchanged. Mix/Elixir-primary still prefers ExUnit over jest. Rails/RSpec-primary still prefers `*_spec.rb` over jest. Python-primary still prefers `test_*.py` over jest. Java-primary still prefers `*Test.java` over jest. C# TFE helpers stay (`deferJsTestSidecarHits` / `deferJsTestSidecarForCsharp` / `isCsharpTestFile` / `isPreferredCsharpTestFile` / `treeHasCsharpManifest` / Fuzz defer). Mix formatter stays. Message-only. Do not dummy `.editorconfig`. Stay L2. No new ids, no L4/L5 retune. Keep #66–#82 and #75 `productFormatterHits` / `deferJsFormatterSidecarHits`. Keep Mix/Rails/Python/Java test-framework ranks.

Hill-climb after PR #83: Ruby/Rails-primary trees may name a JS assets prettier/biome config as `formatter` first-hit even when `.rubocop.yml` / `.rubocop.yaml` / `.standard.yml` exist. Same class as #75 Mix `.formatter.exs` over prettier. When `Gemfile` and a Ruby formatter (`.rubocop.yml` / `.rubocop.yaml` / `.standard.yml`) exist, first-hit prefers those over `prettier.config.*` / `.prettierrc*` / `biome.json`. A Rails tree with only prettier still PASSES on prettier. A JS-only prettier tree is unchanged. Mix `.formatter.exs` over prettier stays. Message-only. Stay L2. No dummy `.editorconfig`. No new ids, no L4/L5 retune. Keep #66–#83 including Mix formatter, Mix/Rails/Python/Java/C# TFE ranks, and test-framework language ranks.

Hill-climb after PR #84: Python-primary trees may name a JS prettier/biome config as `formatter` first-hit even when `ruff.toml` / `[tool.ruff]` / `.black` / pyproject `[tool.black]` exists. Same class as #75 Mix `.formatter.exs` over prettier and #85 Rails `.rubocop.yml` over prettier. When `pyproject.toml` / `ruff.toml` / `.black` exists as a Python formatter hit, first-hit prefers those over `prettier.config.*` / `.prettierrc*` / `biome.json`. A Python tree with only prettier still PASSES on prettier. A JS-only prettier tree is unchanged. Mix `.formatter.exs` over prettier stays. Rails `.rubocop.yml` over prettier stays (`deferJsFormatterSidecarHits` includes `Gemfile` + `isRubyFormatterFile`; catalog `.rubocop.yaml` / `.standard.yml`). C# test-framework over jest stays. Message-only. Do not dummy `.editorconfig`. Stay L2. No new ids, no L4/L5 retune. Keep #66–#85 and #75 `productFormatterHits` / `deferJsFormatterSidecarHits` / `isMixFormatterFile` / `isRubyFormatterFile` / `isJsFormatterSidecar`.

Hill-climb after PR #86: `test-framework` first-hit may name `benchmarks/LegacyTests.cs` even when `tests/FooTests.cs` exists. Fuzz/bench defer was basename-only (`/fuzz|bench/i`); a whole `benchmarks/` path segment was not deferred. Same class as containerization defer of sample/integration path segments. `test-framework` and `test-files-exist` first-hit now defer whole path segments named `benchmarks` / `benchmark` / `bench` / `fuzz` / `fuzzing` / `testdata` / `fixtures` / `samples` / `sample` / `examples` / `example` (case-insensitive; reuses `STYLE_FIRST_HIT_DEFER` / `TEST_FILE_FIRST_HIT_DEFER`). Basename `/fuzz|bench/i` stays. A benchmark-only tree still PASSES and names that file. Message-only. Mix/Rails/Python/Java/C# language ranks stay. Do not dummy `.editorconfig`. Stay L2. No new ids, no L4/L5 retune. Keep #66–#85.

Hill-climb after PR #86: C# trees may name `Lib.TestConsole.csproj` as `setup-script` first-hit even though `Lib.csproj` exists. Same-depth lex: `Lib.` < `Lib/` or `Lib.T` < `Lib.c`. TestConsole is a console harness, not the product library. First-hit among `*.csproj` / `*.sln` now ranks product library (not Tests, not Fuzz/Benchmark, not Console/Demo) over Console/Demo, then `*Tests.csproj`, then Fuzz/Benchmark (`Lib.csproj` over `Lib.TestConsole.csproj`). A Console-only or Demo-only tree still PASSES and names that csproj. Basename match is generic (`Console` / `Demo`); no product-name hardcoding. Keep #69 product over FuzzTests and #83 C# test-framework. mix.exs / CMake first-hit unchanged. Message-only. Do not dummy `.editorconfig`. Stay L2. No new ids, no L4/L5 retune. Keep #66–#86 including `setupDotnetProjectRank` / `preferProductSetupProjects`.

Hill-climb after PR #89: a JS/TS monorepo PASSes `type-checker` on `packages/eslint-plugin-foo/tsconfig.json` even though `packages/foo/tsconfig.json` exists, or on a shallower `hooks/` / `compiler/` tsconfig when the product package config is one segment deeper. Same-depth lex and shallowest-first both miss a product `packages/<name>/tsconfig.json` when a satellite is earlier or shallower, and a missing root tsconfig lets the first nested hit win. First-hit among `tsconfig.json` / `jsconfig.json` (after #65/#67 `test` / `tests` / `spec` / `__tests__` / `fixtures` / `testdata` defer) now ranks repo-root, then `packages/<name>/tsconfig.json` that is not a plugin/satellite, then other product-tree tsconfigs. A path with a whole segment whose name contains `eslint-plugin`, or equals `plugin` / `plugins` / `hooks`, is a worse rank when a better product tsconfig exists (`packages/foo/tsconfig.json` over `packages/eslint-plugin-foo/tsconfig.json`). A plugin-only tree still PASSES and names that tsconfig. A fixtures-only tree still PASSES. mix.exs / C# `languagesPass` for `type-checker` is unchanged. Message-only. Do not dummy `.editorconfig`. Stay L1. No new ids, no L4/L5 retune. Keep #65/#67 test/fixtures defer.
Hill-climb after PR #91: Java `*Test.java` / `*Tests.java` first-hit may name `src/main` API types, `foo-testlib`, mock modules, or sidecar `*_test.py` even when `src/test/java` product tests exist. Lex at the same depth picks `src/main` over `src/test`; a shallow `foo-testlib/src/` or `docker/*_test.py` beats a deeper product test. `test-framework` and `test-files-exist` first-hit among Java tests now prefer consecutive `src/test` over `src/main` and over a bare `src/` that is not under test (`src/test/java/FooTest.java` over `src/main/java/DynamicTest.java`). They also defer a whole path segment whose name is or ends with `testlib` / `mock` / `mocks` / `integration-test` / `integration-tests` / `support-tests` / `support` (case-insensitive), keeping the existing fixtures/testdata/benchmarks/fuzz/samples/examples defer. A testlib-only tree still PASSES and names that file. Java-primary trees (`pom.xml` or `build.gradle` present and a Java test exists) prefer `*Test.java` over sidecar Python `test_*.py` / `*_test.py` (same class as Java-over-JS). A Java tree with only Python tests still PASSES on Python. Mix/Rails/Python/C#/JS sidecar ranks stay. No product-name hardcoding. Message-only. Do not dummy `.editorconfig`. Stay L2. No new ids, no L4/L5 retune. Keep #66–#91 including `typeCheckerConfigRank` / `rankTypeCheckerHits`.

Hill-climb after PR #86: Python trees may name nested `lib/foo/modules/setup.py` as `setup-script` first-hit even though root `pyproject.toml` / `Makefile` exists. Basename `setup.py` matches at any depth and is an `anyFiles` hit, so it ranks before pyproject `[build-system]` (`fileContains`). That file is an application module named setup, not the product installer. First-hit now defers a `setup.py` whose parent path has a whole segment named `modules` / `module` / `plugins` / `plugin` when another product setup file exists (`pyproject.toml` / `setup.cfg` / root `setup.py` / `Makefile` / package.json / `CMakeLists.txt`). A modules-only tree still PASSES and names that file. Same class as preferring product csproj over Console and root CMake over `support/build.gradle`. Keep #86 Console/Demo csproj ranks and #69 Fuzz defer. `go.mod` is still not a setup-script. Message-only. Do not dummy `.editorconfig`. No new ids, no L4/L5 retune. No product-name hardcoding. Keep #91 type-checker product tsconfig rank.

Hill-climb after PR #93: a JS/TS monorepo PASSes `type-checker` on `compiler/apps/playground/tsconfig.json` even though `compiler/packages/foo/tsconfig.json` exists. After #91 defers plugin/hooks/eslint-plugin satellites, remaining product tsconfigs rank by depth/lex, so an `apps` / `playground` demo can beat a nested `packages/<name>/` tsconfig (`compiler/apps` sorts before `compiler/packages` at the same depth). First-hit among `tsconfig.json` / `jsconfig.json` (after #65/#67 `test` / `tests` / `spec` / `__tests__` / `fixtures` / `testdata` defer and #91 satellite defer) now prefers a path with a `packages/<name>/` segment at any depth that is not a plugin/satellite over a path with a whole segment named `apps` or `playground` (`packages/foo/tsconfig.json` over `compiler/apps/playground/tsconfig.json`; `compiler/packages/foo/tsconfig.json` over `compiler/apps/playground/tsconfig.json`). Repo-root `tsconfig.json` still wins first. A playground-only tree still PASSES. A plugin-only tree still PASSES. mix.exs / C# `languagesPass` for `type-checker` is unchanged. Message-only. Do not dummy `.editorconfig`. Stay L1. No new ids, no L4/L5 retune. Keep #65/#67 test/fixtures defer and #91 product-package over plugin satellite.

Hill-climb after PR #93: Java/Kotlin `*Test.java` / `*Test.kt` first-hit may name a satellite hyphen-module `src/test` even when the product module uses a Gradle JVM source set (`src/jvmTest`). Consecutive-segment `src/test` was a better layout rank than `src/jvmTest`; same-depth lex then picks `foo-tls` over `foo` (`-` < `/`). `test-framework` and `test-files-exist` first-hit among Java/Kotlin tests now treat consecutive `src/test` / `src/jvmTest` / `src/androidTest` / `src/androidUnitTest` / `src/commonTest` as the same first-hit class over `src/main` and over a bare `src/` that is not under test. Do not prefer `src/test` over `src/jvmTest`. A satellite hyphen-module (path segment `foo-tls` when sibling `foo` exists) with only `src/test` is not first-hit when a product module has `src/jvmTest` (`foo/src/jvmTest/java/FooTest.java` over `foo-tls/src/test/java/TlsTest.java`). A `src/test`-only tree still PASSES. A jvmTest-only tree still PASSES. testlib/mock/integration-test defer stays. Language-primary JS sidecar defer stays. No product-name hardcoding. Message-only. Do not dummy `.editorconfig`. Stay L2. No new ids, no L4/L5 retune. Keep #66–#94 including Java `src/test` over `src/main` / testlib / sidecar Python (#94) and Go-over-JS (#93).

Hill-climb after PR #93: `test-files-exist` / `test-framework` first-hit may name `integration/` or `e2e/` suites over `packages/<name>/test` at the same depth because lex sorts `integration` before `packages`. Letter-suffix mock/support defer also treated any segment ending in those letters as a mock module. First-hit now defers whole path segments named `integration` / `e2e` / `mock` / `mocks` / `support` (exact, case-insensitive) when another product test exists (`packages/foo/test/foo.spec.ts` over `integration/cors/e2e/express.spec.ts` and over `integration/auto-mock/test/bar.spec.ts`; also `packages/<name>/**/*.spec.ts` / `*.test.ts`). Mock/support is no longer a letter suffix (a segment named `auto-mock` is not a mock segment; exact `mock/` still defers). Trailing hyphen `testlib` / `integration-test` / `integration-tests` / `support-tests` / `processor` / `keeper` stays (`foo-testlib`; `foo/java-test/src/test/java/FooTest.java` over `foo-keeper/src/test/kotlin/KeepTest.kt`). An integration-only, e2e-only, or satellite-only tree still PASSES. Sample/examples/benchmarks defer stays. Mix/Rails/Python/Java/C#/Go language ranks stay. No product-name hardcoding. Message-only. Do not dummy `.editorconfig`. Stay L2. No new ids, no L4/L5 retune. Keep #66–#96 including `preferProductModuleHits` / `isHyphenSatellitePath` (`foo/` over `foo-tls/`) and packages-over-playground type-checker rank.

Hill-climb after PR #97: a JS/TS monorepo PASSes `type-checker` on hyphenated `packages/*plugin*` / `*compiler-plugin*` even though a product `packages/<name>/tsconfig.json` exists. After #91, only a segment containing `eslint-plugin` or exactly `plugin` / `plugins` / `hooks` is a satellite, so `packages/babel-plugin-foo` / `compiler/packages/foo-plugin` still rank as product `packages/<name>/`. First-hit among `tsconfig.json` / `jsconfig.json` now treats a whole path segment whose name contains `plugin` (including hyphenated `foo-plugin`, `babel-plugin-foo`, `compiler-plugin`) as the same satellite class as exact `plugin` / `hooks` (`packages/foo/tsconfig.json` over `packages/babel-plugin-foo/tsconfig.json`; `compiler/packages/foo/tsconfig.json` over `compiler/packages/foo-plugin/tsconfig.json`). Repo-root `tsconfig.json` still wins first. A plugin-only tree still PASSES. `packages/foo` over `apps` / `playground` stays. Exact `plugin` / `hooks` and `test` / `fixtures` / `testdata` defer stay. No product-name hardcoding. Message-only. Do not dummy `.editorconfig`. Stay L1. No new ids, no L4/L5 retune. Keep #65/#67 test/fixtures defer, #91 product-package over plugin satellite, and #95 packages-over-playground.

Hill-climb after PR #99: `src/jvmTest` now ranks with `src/test` (same JVM source-set class, good vs hyphen-tls satellites), but an unsuffixed sibling module with only `src/test` then ties and wins by lex over the product module's `src/jvmTest` (`bar/` < `foo/`). When any hit is under consecutive `src/jvmTest`, `test-framework` and `test-files-exist` first-hit now prefer those over `src/test` hits in other modules (`foo/src/jvmTest/java/FooTest.java` over `bar/src/test/java/BarTest.java`). Do not prefer `src/test` over `src/jvmTest` on the same product module. Hyphen-satellite defer stays (`foo/src/jvmTest` over `foo-tls/src/test`). A `src/test`-only tree still PASSES. A jvmTest-only tree still PASSES. No product-name hardcoding. Message-only. Do not dummy `.editorconfig`. Stay L2. No new ids, no L4/L5 retune. Keep #66–#99 including `preferProductModuleHits` / JVM source-set class.

Hill-climb after PR #103: a JS/TS monorepo PASSes `type-checker` on `packages/foo/tests/types/tsconfig.json` (or `packages/foo/tests/tsconfig.json`) even though `packages/foo/tsconfig.json` or `packages/bar/tsconfig.json` exists. After #95 treated `packages/<name>/` at any depth as the product-package rank, a tsconfig under `packages/<name>/tests/` still counted as that rank. A packages product tsconfig is not first-hit when a whole path segment is `test` / `tests` / `spec` / `__tests__` / `fixtures` / `testdata` if another packages (or root) tsconfig exists outside those segments (`packages/foo/tsconfig.json` over `packages/foo/tests/types/tsconfig.json`; `packages/bar/tsconfig.json` over `packages/foo/tests/tsconfig.json`). Repo-root `tsconfig.json` still wins first. A tests-only tree still PASSES. Keep packages-over-playground (`packages/foo/tsconfig.json` over `compiler/apps/playground/tsconfig.json`; `compiler/packages/foo/tsconfig.json` over `compiler/apps/playground/tsconfig.json`), the #99 plugin-substring satellite (`packages/foo/tsconfig.json` over `packages/babel-plugin-foo/tsconfig.json` and over `packages/eslint-plugin-foo/tsconfig.json`; `compiler/packages/foo/tsconfig.json` over `compiler/packages/foo-plugin/tsconfig.json`), and the #103 jvmTest-over-sibling-src/test rank (`foo/src/jvmTest/java/FooTest.java` over `bar/src/test/java/BarTest.java`). A plugin-only tree still PASSES. A playground-only tree still PASSES. A `src/test`-only tree still PASSES. No product-name hardcoding. Message-only. Do not dummy `.editorconfig`. Stay L1. No new ids, no L4/L5 retune. Keep #65/#67 test/fixtures defer, #91/#99 plugin satellite, #95 packages-over-playground, and #103 jvmTest-over-sibling.

Hill-climb after PR #97: JVM `src/test` / `src/jvmTest` / `src/androidTest` / `src/androidUnitTest` / `src/commonTest` shared one first-hit class, so same-depth lex can name instrumented `src/androidTest` over unit `src/test` (`foo/android-test` < `foo/java-test`). That is a unit-vs-instrumented lie and a regression of a prior `java-test` AGREE. `test-framework` and `test-files-exist` first-hit among Java/Kotlin tests now rank unit source sets (`src/test` / `src/jvmTest` / `src/commonTest` / `src/androidUnitTest`) ahead of instrumented `src/androidTest` (`foo/java-test/src/test/java/FooTest.java` over `foo/android-test/src/androidTest/java/BarTest.java`). Do not prefer `src/test` over `src/jvmTest`. `src/jvmTest` still beats `src/main`. An androidTest-only tree still PASSES. Hyphen-satellite (`foo/` over `foo-tls/`) and testlib/mock/processor/keeper defer stay. No product-name hardcoding (no module-name special case). Message-only. Do not dummy `.editorconfig`. Stay L2. No new ids, no L4/L5 retune. Keep #66–#103 including Java `src/test` over `src/main` / jvmTest-over-satellite (#96), packages-over-integration (#98), hyphenated `*plugin*` type-checker satellites (#99), `src/jvmTest` over other-module `src/test` (#103), and packages-tests tsconfig defer (#100).

Hill-climb after PR #101: `test-files-exist` / `test-framework` first-hit may name a smoke tree under a whole path segment `integration-testing` (or any segment ending with `-testing`) over a product module `src/commonTest`. Exact-segment `integration` / `e2e` defer and hyphen-suffix `integration-test` / `integration-tests` do not match `integration-testing`, so same-class `src/commonTest` then loses on depth/lex. First-hit now defers a whole path segment named `integration-testing` or whose name ends with `-testing` (case-insensitive) when another product test exists (`foo/src/commonTest/kotlin/FooTest.kt` over `integration-testing/smoke/src/commonTest/kotlin/SampleTest.kt`). Exact `integration/` / `e2e/` defer stays. A path that merely contains the letters testing is not deferred (`src/commonTest`). An integration-testing-only tree still PASSES. No product-name hardcoding. Message-only. Do not dummy `.editorconfig`. Stay L2. No new ids, no L4/L5 retune. Keep #66–#101 including exact integration/e2e defer, `integration-test` / `processor` / `keeper` hyphen suffixes, hyphenated `*plugin*` type-checker satellites, `src/jvmTest` over other-module `src/test`, packages-tests tsconfig defer, and unit-JVM over instrumented androidTest.

Hill-climb after PR #102: `src/jvmTest` and `src/commonTest` share the JVM unit source-set class, so same-depth lex names `src/commonTest` over `src/jvmTest` (`c` < `j`). When consecutive `src/jvmTest` and `src/commonTest` hits both exist (same module or any), `test-framework` and `test-files-exist` first-hit now prefer `src/jvmTest` (`foo/src/jvmTest/kotlin/FooTest.kt` over `foo/src/commonTest/kotlin/BarTest.kt`). A commonTest-only tree still PASSES. Do not prefer `src/test` over `src/jvmTest`. Other-module `src/jvmTest` over other-module `src/test` stays (#103). Unit source sets over instrumented `src/androidTest` stay (#101). No product-name hardcoding. Message-only. Do not dummy `.editorconfig`. Stay L1. No new ids, no L4/L5 retune. Keep #66–#102 including jvmTest-over-sibling-src/test, unit-over-androidTest, and integration-testing defer.

Hill-climb after PR #106: `test-files-exist` first-hit may name a catch-all `**/*.test.*` under `docs/` (for example `docs/foo.test.template`) even when language-native tests exist (`**/*Test.kt`, `**/test/**/*Test.kt`, `**/*_test.go`). `javaTestLayoutRank` penalized Kotlin under `common/test/` (not `src/test`) above the catch-all bit, so the template won. Catch-alls are now a worse first-hit class than language-native globs. First-hit also defers a whole path segment named `docs` / `doc` and a `.template` suffix/extension when another product test exists (`foo/common/test/FooTest.kt` over `docs/foo.test.template`; `src/test/java/FooTest.java` over `docs/example.test.js`). A docs-only or template-only tree still PASSES and names that file. `test-framework` first-hit that names a test file follows the same defer; package.json / vitest.config first-hits are unchanged. Keep `src/jvmTest` over `src/commonTest` (#106) and hyphen `util` / `utils` / `internal` type-checker satellites (#105). No product-name hardcoding. Message-only. Do not dummy `.editorconfig`. Stay L1. No new ids, no L4/L5 retune.

Hill-climb after PR #107: `test-files-exist` / `test-framework` first-hit may name a language-native smoke file under `integration-testing/` `src/commonTest` even when product tests sit under consecutive `common/test` with no `src/` (`foo/common/test/FooTest.kt`). The unit source-set layout bit outranked the `integration-testing` / `-testing` defer, so smoke won. Consecutive `common/test` is now the same first-hit class as `src/commonTest` / `src/test`; that defer still applies when another language-native product test exists even if the smoke file sits under `src/commonTest`. `src/commonTest` stays the same class as `src/test` on the same product module. `src/jvmTest` over `src/commonTest` stays (#106). An integration-testing-only, commonTest-only, or common/test-only tree still PASSES and names that file. Language-native globs still beat catch-all `*.test.*`. No product-name hardcoding. Message-only. Do not dummy `.editorconfig`. Stay L1. No new ids, no L4/L5 retune.

Hill-climb after PR #110: `test-files-exist` / `test-framework` first-hit may name a deferred `bench/` leftover (`bench/unit_test.py`) even when a product directory whose exact segment is `testing/` exists (`testing/test_foo.py`). Exact segment `testing` was the same satellite class as hyphen-suffix `-testing` / `integration-testing`, so both `testing/` and `bench/` were deferred and leftover lex picked `bench/` (`b` < `t`). First-hit now requires a hyphen for the `-testing` satellite (or the full token `integration-testing`); an exact path segment `testing` stays a first-hit candidate. Exact `bench` stays deferred with `benchmarks` / `fuzz` so leftover cannot land on `bench/` when a product test dir exists (`testing/test_foo.py` over `bench/unit_test.py`; `src/test_foo.py` over `foo-testing/test_bar.py`). A bench-only tree still PASSES. A testing-only tree still PASSES. Hyphen `foo-testing/` still defers when another product test exists. No product-name hardcoding. Message-only. Do not dummy `.editorconfig`. Stay L1. No new ids, no L4/L5 retune. Keep #66–#110 including exact integration/e2e defer, `integration-testing` / `-testing` hyphen suffixes, and common/test over integration-testing smoke.

Hill-climb after PR #100: a JS/TS monorepo PASSes `type-checker` on a hyphen satellite whose trailing component is `util` / `utils` / `internal` even though another `packages/<name>/tsconfig.json` exists without that suffix. After #99, only a segment containing `plugin` or exactly `plugin` / `plugins` / `hooks` is a satellite, so `packages/make-read-only-util` / `packages/foo-util` still rank as product `packages/<name>/` and can win by lex (`-` < `/`, or an earlier satellite name). First-hit among `tsconfig.json` / `jsconfig.json` now treats a whole path segment that is or has a trailing hyphen component `util` / `utils` / `internal` (case-insensitive) as the same satellite class as plugin/hooks (`packages/foo/tsconfig.json` over `packages/make-read-only-util/tsconfig.json` and over `packages/foo-util/tsconfig.json`). Reuses the test-file exact-or-hyphen helper (`processor` / `keeper`); not a letter suffix (`utils.js` is not `util`). Repo-root `tsconfig.json` still wins first. A util-only tree still PASSES. Keep plugin-substring satellite (`packages/foo/tsconfig.json` over `packages/babel-plugin-foo/tsconfig.json` and over `packages/eslint-plugin-foo/tsconfig.json`; `compiler/packages/foo/tsconfig.json` over `compiler/packages/foo-plugin/tsconfig.json`), packages-over-playground (`packages/foo/tsconfig.json` over `compiler/apps/playground/tsconfig.json`; `compiler/packages/foo/tsconfig.json` over `compiler/apps/playground/tsconfig.json`), tests-path packages defer (`packages/foo/tsconfig.json` over `packages/foo/tests/types/tsconfig.json`; `packages/bar/tsconfig.json` over `packages/foo/tests/tsconfig.json`), and `src/jvmTest` over `src/commonTest` when both exist (`foo/src/jvmTest/kotlin/FooTest.kt` over `foo/src/commonTest/kotlin/BarTest.kt`). A plugin-only tree still PASSES. A playground-only tree still PASSES. A tests-only tree still PASSES. A commonTest-only tree still PASSES. No product-name hardcoding. Message-only. Do not dummy `.editorconfig`. Stay L1. No new ids, no L4/L5 retune. Keep #65/#67 test/fixtures defer, #91/#99 plugin satellite, #95 packages-over-playground, #100 tests-path packages defer, and #106 jvmTest-over-commonTest.

Hill-climb after PR #105: a JS/TS monorepo PASSes `type-checker` on a hyphen satellite whose trailing component is a CLI-bin/tooling role (`healthcheck` / `cli` / `bin` / `cmd` / `tool` / `tools`) even though another `packages/<name>/tsconfig.json` exists without that suffix. After #105, only `util` / `utils` / `internal` (plus plugin/hooks) are satellites, so `packages/foo-healthcheck` still ranks as product `packages/<name>/` and can win by lex over `packages/foo-runtime` (`h` < `r`). First-hit among `tsconfig.json` / `jsconfig.json` now treats a whole path segment that is or has a trailing hyphen component `healthcheck` / `cli` / `bin` / `cmd` / `tool` / `tools` (case-insensitive) as the same satellite class as `util` (`packages/foo-runtime/tsconfig.json` over `packages/foo-healthcheck/tsconfig.json`; `packages/foo/tsconfig.json` over `packages/foo-cli/tsconfig.json` and over `packages/healthcheck/tsconfig.json`). Reuses the test-file exact-or-hyphen helper; not a letter suffix (`healthcheck.js` is not `healthcheck`). Repo-root `tsconfig.json` still wins first. A healthcheck-only tree still PASSES. Keep hyphen `util` / `utils` / `internal` (#105), plugin-substring satellite, packages-over-playground, and tests-path packages defer. No product-name hardcoding. Message-only. Do not dummy `.editorconfig`. Stay L1. No new ids, no L4/L5 retune. Keep #65/#67 test/fixtures defer, #91/#99 plugin satellite, #95 packages-over-playground, #100 tests-path packages defer, #105 util satellites, and #106 jvmTest-over-commonTest.

Hill-climb after PR #110: a JVM-primary repo (Scala/Kotlin/Java) PASSes `type-checker` on leftover `website/plugins/*/tsconfig.json` or `website/tsconfig.json` even though the product is a JVM language with a built-in type system (`build.sbt` / `*.scala` / `pom.xml` / `build.gradle` / `build.gradle.kts`). Plugin satellites already rank worse than product packages, but leftover still names the website docs plugin when it is the only tsconfig. First-hit among `tsconfig.json` / `jsconfig.json` now treats a whole path segment named `website` / `docs` / `doc` (case-insensitive, exact segment) as the same satellite class as plugin (`packages/foo/tsconfig.json` over `website/tsconfig.json` and over `website/plugins/site-plugin/tsconfig.json`). When Scala/Kotlin/Java sources or those JVM build files are present, leftover website / docs / plugin tsconfig is not first-hit: Java/Kotlin auto-pass via the built-in type system when a language manifest is present; Scala without a conventional checker file skips. A website-only TypeScript tree still PASSES. Keep plugin-substring satellite (`packages/foo/tsconfig.json` over `packages/foo-plugin/tsconfig.json`). No product-name hardcoding. Message-only. Do not dummy `.editorconfig`. Stay L1. No new ids, no L4/L5 retune.

## Explicitly refused

- New criterion ids
- New pillars (observability, task discovery, product, build-system)
- LLM checks (`v1SkipLLM`; L5 quality rows stay skipped)
- Moving `branch-protection` to L2 (detector is a weak file mention; stays L4)
- Moving `containerization` to L2 (Dockerfile false-pass; stays L3 Standardized). Factory's L2 devcontainer is a subset of this detector.
- Moving `e2e-tests` to L3 (Factory L3 integration tests). Our detector is Playwright/Cypress, not generic integration; stays L4.
- L4/L5 threshold or id retune beyond ids that moved away
- Canvas chrome / layout (level label strings only)

## Unchanged mechanics

- Sequential 80% gate, skipped rows out of the denominator
- Repository-root walk
- Seven pillars
- Python-native test-script / setup-script detectors
- `env-documentation` skip unless root env/compose/direnv signals exist
- `lock-file` language-aware skip-when-absent (now an L2 skip)
- `editorconfig` skip when `linter` already passes and `.editorconfig` is missing (L2 denominator drop; still fail if no linter, still pass if the file exists)
